export type StateFormat = {
    cookies: number,
    cps: number,
    amount_owned: number[],
    shops: any,
};

export type ItemFormat = {
    cost: number,
    clicks_per_second:number,
    id: number,
};